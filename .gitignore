# --- secrets (NEVER commit) ---
.sia_hf_token
*.hf_token
hf_token*
.streamlit/secrets.toml
.env

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# --- os / editor ---
.DS_Store
.claude/

# --- large model artifacts (regenerated by train_pipeline.py) ---
artifacts/models/
*.safetensors
*.bin
*.pt

# --- processed data caches (regenerated; raw CSV is tracked) ---
artifacts/data/*.parquet
artifacts/data/*.pkl

# keep metrics, ablation tables, dossiers (small, useful in repo)
!artifacts/metrics/

# --- raw dataset with customer PII (names/emails) — NOT for public repo ---
# Download from kaggle.com/datasets/ajverse/customer-support-tickets-crm-dataset
data/customer_support_tickets.csv
data/enhanced_customer_support_data.csv
